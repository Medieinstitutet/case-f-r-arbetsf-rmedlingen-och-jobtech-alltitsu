import { createContext } from 'react';
export const SetToDateContext = createContext(() => {});
export const SetFromDateContext = createContext(() => {});
export const SetCompanyContext = createContext(() => {});
